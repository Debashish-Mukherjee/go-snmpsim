apiVersion: 1

providers:
  - name: 'SNMP Simulator Dashboards'
    orgId: 1
    folder: 'SNMP'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
